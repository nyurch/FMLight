<!-- image properties -->
<properties>

	<!-- image borders -->
	<!-- these determine how many pixels any content is inset from each side of the image -->
	<!-- (for example, in the case of a button, the content would be the text label) -->
	<record id="image_borders" left="15" right="15" top="0" bottom="0"/>

	<!-- image slices determine the points at which the image will be sliced to enable scaling -->
	<!-- the image will only slice in the directions slicing values are provided for-->
	<record id="image_slices" left="10" right="2" top="2" bottom="10"/>
	
	<record id="font_spec">
		<flags id="font" value="fonts/button"/> 
	</record>

	<colour id="red_replacement" name="bg accent"/>
	<colour id="blue_replacement" name="bg"/>

	<record id="primary_icon_properties" red_replacement="bg" green_replacement="button_icon"/>
	<record id="secondary_icon_properties" red_replacement="bg" green_replacement="button_icon"/>
	
</properties>
